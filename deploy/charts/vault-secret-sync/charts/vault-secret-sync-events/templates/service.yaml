apiVersion: v1
kind: Service
metadata:
  name: {{ include "secretsync-events.fullname" . }}
  labels:
    {{- include "secretsync-events.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "secretsync-events.selectorLabels" . | nindent 4 }}