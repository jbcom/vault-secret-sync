{{- if (eq .Values.existingConfigMap "") }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "secretsync-operator.configMapName" . }}
  labels:
    {{- include "secretsync-operator.labels" . | nindent 4 }}
data:
  config.yaml: |
  {{- .Values.config | toYaml | nindent 4 }}
{{- end }}